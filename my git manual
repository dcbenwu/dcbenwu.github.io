1， create a repo on git web client
2, create a directory on local windows
3, clone the new created repo with command "git clone https://xxxxx"
4, add files in the new local dir
5, add the new files to local repo with command "git add ."
6, commit them with command "git commit -m "xxx" "
7, push them to remote server with command "git push"


then, you can pull them to any workbench
